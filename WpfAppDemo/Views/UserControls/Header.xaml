<UserControl x:Class="WpfAppDemo.Views.UserControls.Header"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ns="clr-namespace:WpfAppDemo" Height="80">
    <UserControl.Resources>
    </UserControl.Resources>
    <Grid Background="{StaticResource appsolidbrushheader}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" >
            <TextBlock Text="{StaticResource HeaderTitle}" Foreground="{StaticResource customsolidbrushheader}">
                
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>
