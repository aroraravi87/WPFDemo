<UserControl x:Class="WpfAppDemo.Views.UserControls.Footer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="100">
    <UserControl.Resources>
        
    </UserControl.Resources>
    <Grid Background="{StaticResource appsolidbrushheader}">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" >
            <TextBlock Text="{StaticResource FooterTitle}" Foreground="{StaticResource customsolidbrushfooter}">
            </TextBlock>
        </StackPanel>
        
    </Grid>
</UserControl>
